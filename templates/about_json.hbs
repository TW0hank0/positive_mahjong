{
"generated_at": "{{now}}",
"project_name": "{{crate.name}}",
"project_version": "{{crate.version}}",
"dependencies": [
{{#each licenses}}
{
"name": "{{crate.name}}",
"version": "{{crate.version}}",
{{#each used_by}}
{{/each}}
"license_id": "{{id}}",
"license_text": {{json text}},
"repository": "{{#if crate.repository}} {{crate.repository}} {{else}} https://crates.io/crates/{{crate.name}}{{/if}}"
}{{#if @last}}{{else}},{{/if}}
{{/each}}
]
}