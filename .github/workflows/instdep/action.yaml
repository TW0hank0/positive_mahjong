# SPDX-License-Identifier: AGPL-3.0-only
# 著作權所有 (C) 2026 TW0hank0
#
# 本檔案屬於 positive_mahjong 專案的一部分。
# 專案儲存庫：https://gitlab.com/TW0hank0/positive_mahjong
#
# 本程式為自由軟體：您可以根據自由軟體基金會發佈的 GNU Affero 通用公共授權條款
# 第 3 版（僅此版本）重新發佈及/或修改本程式。
#
# 本程式的發佈是希望它能發揮功用，但不提供任何擔保；
# 甚至沒有隱含的適銷性或特定目的適用性擔保。詳見 GNU Affero 通用公共授權條款。
#
# 您應該已經收到一份 GNU Affero 通用公共授權條款副本。
# 如果沒有，請參見 <https://www.gnu.org/licenses/>。

name: Install Dep

description: Install dep that workflows need

runs:
  using: composite
  steps:
    - name: Install uv
      uses: astral-sh/setup-uv@v7

    - name: check uv install
      shell: pwsh
      run: |
        uv self version

    - name: install rustup on Windows
      if: runner.os == 'Windows'
      shell: pwsh
      run: |
        Invoke-WebRequest -Uri "https://win.rustup.rs" -OutFile "rustup-init.exe"
        ls
        .\rustup-init.exe -y --profile minimal

    - name: install rustup on Linux
      shell: pwsh
      if: runner.os == 'Linux'
      run: |
        curl https://sh.rustup.rs -o rust-init.sh
        sh rust-init.sh -y
        rustup target add x86_64-unknown-linux-musl

    - name: check rust Install
      shell: pwsh
      run: |
        rustup --version
        rustc --version
        cargo --version

    - name: install rust toolchain
      shell: pwsh
      run: |
        rustup update

    - name: install Linux build needed dep
      shell: pwsh
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install libfontconfig1-dev pkg-config mold gcc -y

    - name: install go
      uses: actions/setup-go@v6
      with:
        go-version: "stable"

    - name: use go to install addlicense tool
      shell: pwsh
      run: |
        go install github.com/google/addlicense@latest
        echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

    - name: use cargo to install tools
      shell: pwsh
      run: |
        cargo install cargo-audit cargo-msrv cargo-deny taplo-cli mdbook --locked
        cargo install cargo-about --features="cli" --locked

    - name: check tools installion
      shell: pwsh
      run: |
        cargo about --version
        cargo audit --version
        cargo msrv --version
        cargo deny --version
        taplo --version
        mdbook --version

    - name: install java
      uses: actions/setup-java@v5
      with:
        distribution: "temurin" # See 'Supported distributions' for available options
        java-version: "21"

    - name: Setup Android
      uses: android-actions/setup-android@v4
